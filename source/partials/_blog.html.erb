<%# Merged posts: local blog articles + data.blog, sorted by date (newest first) %>
<% merged_blog_posts.each do |entry| %>
  <%= partial "partials/blog_post_card", locals: { entry: entry } %>
<% end %>
