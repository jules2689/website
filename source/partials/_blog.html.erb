<% data.blog.each do |post| %>
   <%=
      partial "partials/card", locals: { 
        link: post.url,
        image: post.social_image,
        year: post.readable_publish_date,
        type: URI(post.url).hostname,
        title: post.title,
        description: post.description[0..140] + "..."
      }
    %>
<% end %>
