# Ralph Progress Log
Started: Sun Feb 15 21:03:05 EST 2026

## Codebase Patterns
- Optional partial locals: In Middleman ERB partials use `defined?(variable_name)` to check for optional locals; `local_assigns` is not available (Rails-only).
- Sticky footer: Body uses `min-height: 100vh; display: flex; flex-direction: column`. Main content (e.g. `.page-content`) gets `flex: 1 1 auto`; footer gets `flex-shrink: 0` and minimal padding so it stays at bottom and content fills the rest.
- Blog: Use middleman-blog; articles live in `source/blog/` as `YYYY-MM-DD-slug.html.markdown` with YAML front matter (`title`, optional `date`, `description`, `social_image`). For a single date-sorted list of local + external posts, use the `merged_blog_posts` helper (config.rb `helpers do`); render each entry with `partial "partials/blog_post_card"` and `locals: { entry: entry }`.
- Blog index: With prefix "blog", the blog index template is `source/blog/index.html.erb` (served at `/blog/`). Use `page-header` + `partial "partials/nav"` and the same `.section-label`, `.section-title`, `.cards` + card partial as the home page for consistent layout.
- Inner pages: Use `page-header` (accent background), `partial "partials/nav"` (Home + Blog), `page-title`, `page-subtitle`, then `section.page-content` with `.wrapper`. Nav partial supports multiple links; style with `.page-back` (flex gap).
- Data: Use `data/homepage.yaml` for highlights cards, `data/blog.json` for external writing links. Load in templates as `data.homepage`, `data.blog`.
- Partials: Live in `source/partials/` with leading `_`; render with `partial "partials/name"`. Card partial expects `link`, `image`, `year`, `type`, `title`, optional `description`, `image_title`, `image_attributes`.
- Quality: No Rubocop in Gemfile; `bundle exec middleman build` is the main verification. npm test is a placeholder (exit 1).

---
## 2026-02-16 - 001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented blog platform: markdown posts with front matter rendered as posts; posts auto-added to Writing section on home page.
- Files changed: Gemfile (middleman-blog), config.rb (activate :blog), source/blog/2026-02-15-welcome-post.html.markdown (sample post), source/partials/_blog.html.erb (blog.articles + data.blog), source/AGENTS.md (new).
- **Learnings for future iterations:**
  - middleman-blog expects articles under `source/blog/` when prefix is "blog"; sources pattern is `{year}-{month}-{day}-{title}.html` (file extension is separate, e.g. .markdown).
  - Writing section shows local blog articles first, then data.blog; card partial needs description to be plain text (strip HTML from article.summary when no front matter description).
  - Build succeeds; dev server may fail if port 4567 is in use (environment-specific).
---
## 2026-02-16 - 002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented blog page redesign: blog index at `/blog/` with navigation, page-header, and card layout consistent with home page; blog articles use blog_layout with nav, title, date, and page-content; added Blog link to home hero and nav partial (Home + Blog).
- Files changed: source/blog/index.html.erb (new), source/partials/_nav.html.erb (Home + Blog links), source/partials/_header.html.erb (Blog in hero-links), source/layouts/blog_layout.erb (new), config.rb (blog.layout = blog_layout), source/AGENTS.md, assets/stylesheets/site.css.scss (page-back flex), progress.txt, prd.json.
- **Learnings for future iterations:**
  - With blog.prefix = "blog" and directory_indexes, the blog index is source/blog/index.html.erb; middleman-blog does not auto-create it—add the file so /blog/ exists. Article layout is controlled by blog.layout in config; use current_article in the layout for article title/date.
  - Inner pages use page-header + partial "partials/nav"; nav partial can contain multiple links (e.g. ← Home, Blog); .page-back flex gap styles them in a row.
  - Browser verification: Playwright MCP cannot access localhost or file://; build output was inspected to confirm blog index and article pages render correctly.
---
## 2026-02-16 - 003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Fixed blog page footer: footer is now fixed at bottom with minimal height; main content (page-content) fills the rest via flex layout.
- Files changed: assets/stylesheets/site.css.scss (body flex column, min-height 100vh; .page-content flex: 1 1 auto; footer flex-shrink: 0, padding 16px), source/AGENTS.md (sticky footer pattern), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Sticky footer: use body as flex column with min-height 100vh; give the main content wrapper (e.g. .page-content) flex: 1 1 auto and footer flex-shrink: 0 with small padding so content takes majority and footer stays at bottom.
  - Same layout applies to both blog index and article pages since both use blog_layout and include a section with class page-content.
---
## 2026-02-16 - 004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Mixed manually entered blog posts (data/blog.json) into the blog as pseudo posts: single merged list with local middleman-blog articles, sorted by date (newest first). External posts link out to their host (target="_blank"); local posts use relative URLs.
- Files changed: config.rb (helpers: merged_blog_posts, parsed_external_post_date), source/partials/_blog.html.erb (use merged_blog_posts + blog_post_card), source/partials/_blog_post_card.html.erb (new), source/blog/index.html.erb (use merged_blog_posts + blog_post_card), source/AGENTS.md (merged-posts pattern), scripts/ralph/progress.txt (Codebase Patterns + log), scripts/ralph/prd.json (004 passes: true).
- **Learnings for future iterations:**
  - Use `helpers do ... end` in config.rb so methods are available in ERB; `merged_blog_posts` returns array of { date:, local: true/false, article: or post: }.
  - External post dates: prefer published_timestamp/published_at (ISO), fallback to readable_publish_date (parsed with Time.parse); use both symbol and string keys for data.blog entries.
  - Blog index and home Writing section both use the same helper and partial for consistent, date-sorted display; no separate "local then external" blocks.
---
## 2026-02-15 - 005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added external-link icon for external blog posts on the blog index only; homepage Writing section unchanged (no icons).
- Files changed: source/partials/_card.html.erb (optional external_icon, inline SVG), source/partials/_blog_post_card.html.erb (show_external when show_external_icon passed), source/blog/index.html.erb (pass show_external_icon: true), assets/stylesheets/site.css.scss (.card-title-external, card-title flex), source/AGENTS.md (optional locals + external icon note), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Middleman ERB partials do not have Rails' local_assigns; use defined?(var_name) to check optional locals to avoid NameError during build.
  - To show context-dependent UI in a shared partial (e.g. icon on blog index but not homepage), pass an optional local from the calling template (e.g. show_external_icon: true) and guard with defined? in the partial.
---
## 2026-02-16 - 006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Made the blog index page three columns instead of two: `.blog .cards` now uses `grid-template-columns: repeat(3, 1fr)` with responsive breakpoints (2 cols at 900px, 1 col at 560px). Widened blog section wrapper to 1100px so three columns fit.
- Files changed: assets/stylesheets/site.css.scss (blog .cards three-column grid + media queries, section.blog.page-content .wrapper max-width 1100px), source/AGENTS.md (blog index three-column note), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Blog index card grid is scoped with `.blog .cards`; override `.page-content .wrapper` for blog only with `section.blog.page-content .wrapper` so the cards container is wide enough for three columns (1100px matches main .wrapper).
  - Dev server failed (port 4567 in use); build output and CSS structure were used to verify. Playwright cannot access localhost per progress log.
---
